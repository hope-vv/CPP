1. Create a base class (Base) with a virtual destructor.
2. Create three derived classes (A, B, C) that inherit from Base.
3. Implement functions that detect the actual type of an object pointed or referenced as a Base.